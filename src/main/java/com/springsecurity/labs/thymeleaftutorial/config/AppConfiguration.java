package com.springsecurity.labs.thymeleaftutorial.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AppConfiguration {

/*    @Bean
    public PlayerService playerService(PlayerRepository playerRepository, TeamRepository teamRepository) {
        return new PlayerServiceImpl(playerRepository, teamRepository);
    }*/

/*    @Bean
    public CommandLineRunner commandLineRunner(PlayerRepository playerRepository,
                                               TeamRepository teamRepository,
                                               LeaguesRepository leaguesRepository,
                                               ConferenceRepository conferenceRepository,
                                               DivisionsRepository divisionsRepository) {
        return new DbInitializer( playerRepository,
                teamRepository,
                leaguesRepository,
                conferenceRepository,
                divisionsRepository);
    }*/
}
